name: xtensor-python
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - ninja
  # Host dependencies
  - xtensor>=0.27,<0.28
  - numpy>=2.0
  - pybind11>=2.12.0,<4
  # Test dependencies
  - setuptools
  - pytest

