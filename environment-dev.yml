name: xtensor-python
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - ninja
  # Host dependencies
  - xtensor>=0.24,<0.25
  - numpy
  - pybind11>=2.6.1,<3
  # Test dependencies
  - pytest

